This is a discord moderation bot. To start, run the docker container.

Features:
PostgreSQL for storing bans, notes, warnings for all users
Moderation commands to enforce this including ban for x amount of time
Embed UI support
Chat logs monitoring message edits and message deletes.
